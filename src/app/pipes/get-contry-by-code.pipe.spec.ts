import { GetContryByCodePipe } from './get-contry-by-code.pipe';

describe('GetContryByCodePipe', () => {
  it('create an instance', () => {
    const pipe = new GetContryByCodePipe();
    expect(pipe).toBeTruthy();
  });
});
