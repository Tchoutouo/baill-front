import { FormatTimeSimplePipe } from './format-time-simple.pipe';

describe('FormatTimeSimplePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimeSimplePipe();
    expect(pipe).toBeTruthy();
  });
});
